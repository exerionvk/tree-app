import React from 'react';
import TreeContainer from './containers/TreeContainer';
import './styles/global.scss';

const App: React.FC = () => {
  return (
      <TreeContainer />
  );
};

export default App;